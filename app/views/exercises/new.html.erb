<!DOCTYPE html>
<html lang="ja">
<head>
  <meta charset="UTF-8">
  <title>トレーニング記録</title>
  <!-- スタイルシートへのリンク -->
  <link rel="stylesheet" href="path_to_your_stylesheet.css">
</head>
<body>
  <div class="form-container">
    <h2 class="form-title">部位を追加</h2>
    <%= form_with(model: @exercise, local: true, class: "exercise-form") do |form| %>
      <div class="form-group">
        <%= form.label :part, '部位:', class: "form-label" %>
        <%= form.text_field :part, class: "form-control" %>
      </div>
      <div class="form-group">
        <%= form.label :exercise, '種目:', class: "form-label" %>
        <%= form.text_field :exercise, class: "form-control" %>
      </div>
      <!-- 日付フィールドの追加 -->
      <div class="form-group">
        <%= form.label :date, '日付:', class: "form-label" %>
        <%= form.date_field :date, class: "form-control", value: @exercise.date || Date.today.to_s %>
      </div>
      <div class="form-group">
        <%= form.submit '追加', class: "btn btn-primary" %>
      </div>
    <% end %>
  </div>
</body>
</html>